<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="6dp">

    <ImageView
        android:id="@+id/imageViewFotoVideojuego"
        android:layout_width="140dp"
        android:layout_height="180dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/titanquestcaratula">
    </ImageView>

    <TextView
        android:id="@+id/tituloVideojuego"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Titulo"
        android:textColor="@color/white"
        android:textSize="12sp"
        android:lines="2"
        android:textAlignment="center"
        android:maxLines="2"
        android:layout_marginTop="5dp"
        app:layout_constraintTop_toBottomOf="@id/imageViewFotoVideojuego"
        app:layout_constraintStart_toStartOf="@id/imageViewFotoVideojuego"
        app:layout_constraintEnd_toEndOf="@id/imageViewFotoVideojuego">
    </TextView>


</androidx.constraintlayout.widget.ConstraintLayout>